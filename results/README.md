# Evaluation Results

This directory contains small, reproducible summaries of model accuracy and
runtime performance. These files are tracked by Git.

Large generated artifacts, including annotated images and per-image prediction
JSON files, are stored under `outputs/` and are not tracked.
